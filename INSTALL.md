To install the python_pdf_extractor package locally, run:

    python -m pip install ./python_pdf_extractor

This directory contains the package, pyproject.toml, and setup.cfg for
building the wheel.
